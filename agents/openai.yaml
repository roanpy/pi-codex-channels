interface:
  display_name: "Pi Codex Channels"
  short_description: "Delegate bounded tasks to named Pi channels with resumable sessions"
  default_prompt: "Use $pi-codex-channels to pick the right channel for the current task, run it in one-shot prompt mode, and keep the session resumable."
